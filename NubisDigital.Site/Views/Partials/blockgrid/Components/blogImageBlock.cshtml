@using Umbraco.Cms.Core.Models.Blocks
@using Umbraco.Cms.Core.Models.PublishedContent
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<BlockGridItem>
@{
    var image = Model.Content.Value<IPublishedContent>("image");
    var caption = Model.Content.Value<string>("caption");
    var imageUrl = image?.Url() ?? "";
    var altText = image?.Value<string>("altText") ?? caption ?? "Blog image";
}

@if (!string.IsNullOrEmpty(imageUrl))
{
    <figure class="my-8">
        <img src="@imageUrl" alt="@altText" class="w-full rounded-lg shadow-lg" />
        @if (!string.IsNullOrEmpty(caption))
        {
            <figcaption class="mt-3 text-sm text-center text-gray-600 dark:text-gray-400 italic">
                @caption
            </figcaption>
        }
    </figure>
}